# 对照组诊断记录（冷启动 Claude Sonnet 子 Agent）

> 输入：原始 trace 从头截断到 512,000 字节（≈128K token，正好填满窗口），即「把 trace
> 塞进上下文、超出部分被硬截断」的常规做法。同一款子 Agent，同一句任务，事先不知道 bug。
> 以下为其原样交付的结论（已把本机绝对路径改为相对形式）。

## 0. 窗口内数据总览
- 文件在第 309,544 字符处被硬截断，JSON 不完整、无法完整 parse。
- 窗口内可解析出 **57 个 span**：`research_agent`(根) → `turn_1/2/3` 各含 1 个 LLM + 15 个
  `web_search`，`turn_4` 只露出链节点 + 1 个 LLM + 3 个 web_search 就被截断。
- 完整覆盖第 1–3 轮，第 4 轮只看到 20%；第 5–24 轮（约 355 个 span，占 86%）完全不在窗口内。
- 全窗口 `status_code` 只出现 `"OK"`（**57 次，0 次 ERROR**）；关键词 `error` 全文命中 **0 次**。

## 1. 根因 span
**无法在窗口内定位。** 没有任何 span 状态为 ERROR，也没有任何错误文本或错误码。

## 2. 确切错误
**窗口内没有错误信息原文，也没有错误码。** 唯一「异常」是语义层面的：所有 web_search 返回
的是同一小撮低信息量套话随机重排拼接的长文本，从未出现具体营收数字。这是「内容存疑」的
间接线索，但不构成可确认的根因。

## 3. 掩盖 / 传播机制（可判断部分）
- 每个 web_search 的 `spanOutputs` 长约 6,000–6,400 字符，56 个可见 web_search 就占了窗口
  总字节的 60%–70%，信息熵极低。
- 系统把前几轮完整工具输出原样拼回下一轮对话历史（全历史累积，非摘要），噪声逐轮膨胀。
- 结果：即便真正根因出现在第 5–24 轮，「头部截断 + 逐轮噪声堆积」几乎必然把它挤出窗口。
  本案例窗口只够看到 24 轮里的 3.1 轮（13%）。

## 4. 修复建议
- 排障方法上：不要对 trace 做字节头部截断；应按 span 索引优先抓 `status != OK`、各轮摘要
  节点、以及产出最终答案的那几轮。
- 系统上：工具输出加摘要压缩、对话历史滑动窗口化、底层错误必须显式落到 `status_code=ERROR`。

## 5. 诚实评估
**不能确定根因。** 窗口内 57 个 span、0 个 ERROR、0 次 error 关键词命中；根因所在的第 20 轮
和产出最终答案的那一轮完全缺失。只能定性指出一个强可疑诱因（噪声稀释、根因被截断在窗口外），
**给不出确定的根因 span、错误原文或错误码**。要给出确定结论，至少需要能看到第 5–24 轮。
